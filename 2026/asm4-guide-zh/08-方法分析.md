# 目录

目录：[ASM4中文指南](2026/asm4-guide-zh/README.md)

# 8. 方法分析

本章介绍用于分析方法代码的 ASM API，它基于 Tree API。本章首先介绍代码分析算法，然后介绍相应的 ASM API，并给出一些示例。

> 笔记：
>
> 先看本章的角色变化：第 7 章用 Tree API 改方法，这一章用 Tree API 看懂方法。原文先讲两类分析思路——数据流和控制流，再讲 ASM 提供的 `Analyzer`、`Frame`、`Interpreter`、`Value` 怎样把这些思路落到方法指令列表上。
>
> 由此得出一个阅读顺序：先弄清“帧状态怎么沿指令传播”和“控制流图怎么连边”，再看几个解释器从粗到细地检查死代码、类型错误、空指针和圈复杂度。

## 8.1 展示

代码分析是一个非常大的主题，可用于分析代码的算法非常多。在这里把它们全部介绍一遍既不可能，也超出了本文档的范围。事实上，本节的目的只是概述 ASM 中所使用的算法。关于这一主题更好的介绍可以在编译原理相关的书籍中找到。接下来的几节介绍两类重要的代码分析技术，即数据流分析和控制流分析：

- 数据流分析是指针对方法的每条指令，计算该方法执行帧的状态。这个状态可以用抽象程度不同的方式来表示。例如，引用值可以用单个值来表示，可以每个类用一个值来表示，也可以用在 { null, not null, may be null } 集合中的三个可能值来表示，等等。
- 控制流分析是指计算方法的控制流图，并在这个图上进行分析。控制流图是这样一种图：它的节点是指令，它的有向边连接两条指令 i → j，当且仅当 j 可以在 i 之后紧接着被执行。

> 笔记：
>
> 先看 8.1 的边界：它不打算介绍所有代码分析算法，只概述 ASM 后面要用的两类。第一类是数据流分析，关心每条指令处的帧状态；第二类是控制流分析，关心哪些指令可能接着哪些指令执行。
>
> 由此得出阅读顺序：先分清“算状态”和“连边”这两件事，再进入两个小节分别看。

### 8.1.1 数据流分析

可以执行两类数据流分析：

- 前向分析针对每条指令，根据该指令执行之前的状态，计算该指令执行之后执行帧的状态。
- 后向分析针对每条指令，根据该指令执行之后的状态，计算该指令执行之前执行帧的状态。

前向数据流分析是通过在方法的执行帧上模拟该方法每条字节码指令的执行来完成的，这通常包括：

- 从栈中弹出值，
- 把它们组合起来，
- 再把结果压入栈中。

这看起来像是解释器或 Java 虚拟机所做的事情，但实际上完全不同，因为其目标是模拟方法中所有可能的执行路径（对应所有可能的参数值），而不是由某些特定方法参数值所确定的那一条执行路径。一个后果是，对于分支指令，两个分支都会被模拟（而真正的解释器只根据实际的条件值沿一个分支执行）。

另一个后果是，被操作的值实际上是可能值的集合。这些集合可能非常大，例如「所有可能的值」「所有整数」「所有可能的对象」或「所有可能的 `String` 对象」，在这种情况下它们也可以被称为类型。它们也可以更精确，例如「所有正整数」「0 到 10 之间的所有整数」或「所有可能的非 null 对象」。模拟一条指令 i 的执行，就是针对其操作数值集合中所有值的组合，求出 i 的所有可能结果所构成的集合。例如，如果用三个集合表示整数：P =「正数或 null」，N =「负数或 null」，A =「所有整数」，那么模拟 `IADD` 指令就意味着：如果两个操作数都是 P 则返回 P，如果两个操作数都是 N 则返回 N，其余所有情况返回 A。

最后一个后果是需要计算值集合的并集：例如，与 `(b ? e1 : e2)` 对应的可能值集合，是 `e1` 的可能值与 `e2` 的可能值的并集。更一般地说，只要控制流图中存在两条或多条边具有共同的目标，就需要进行这一运算。在前面的例子中，整数用 P、N、A 三个集合表示，计算其中两个集合的并集很容易：除非这两个集合相等，否则结果总是 A。

> 笔记：
>
> 先看 8.1.1，它是第 8 章的地基，抓住三点。第一，**前向分析**从方法入口往下走、逐条模拟执行帧；**后向分析**反过来从出口往上推——后面 8.2 节实际用到的框架全是前向的，先记住这一个就够了。
>
> 第二，模拟的目的不是执行某一条路径，而是**把方法所有可能的执行路径全走一遍**：分支的两个分支都要模拟，循环要靠不断迭代、直到帧不再变化（这叫“不动点”）才结束。这是理解后面 `Analyzer` 行为的前提。
>
> 第三，被操作的值其实是**可能值的集合**，合并靠**并集**。P/N/A 三个整数集合的例子就是「抽象解释」的雏形：两个 P 得 P、两个 N 得 N、其余情况凑出 A。你只需记住：两条边汇合到同一指令时，都要对两边取值做并集——后面所有 `Interpreter` 子类，归根结底都在回答“这个并集怎么算”。

### 8.1.2 控制流分析

控制流分析是一种基于方法控制流图的分析。作为一个例子，3.1.3 节中 `checkAndSetF` 方法的控制流图如下所示（图中把标签也像真实指令一样包含进来）：

![原书第 117 页插图](images/figure-p117.png)

这个图可以分解为四个基本块（在上图中用矩形表示）。基本块是这样一个指令序列：其中除最后一条指令外，每条指令都恰好有一个后继，并且其中除第一条指令外，任何指令都不能是跳转的目标。

> 笔记：
>
> 先看 8.1.2 的图：节点是指令，边表示“下一步可能执行到哪条指令”。普通顺序执行是一条向下的边，条件跳转会分出多条边，异常或返回会改变后继关系。
>
> 再看基本块定义：块内部每条指令都顺着唯一后继往下走，只有最后一条指令负责分叉或结束。由此得出后面控制流分析的入口：只要能记录这些边，就能在图上计算复杂度这类性质。

## 8.2 接口与组件

用于代码分析的 ASM API 位于 `org.objectweb.asm.tree.analysis` 包中。正如包名所暗示的，它基于 Tree API。事实上，这个包提供了一个执行前向数据流分析的框架。为了能够用抽象程度不同的值集合执行各种数据流分析，数据流分析算法被拆分为两部分：一部分是固定的，由框架提供；另一部分是可变的，由用户提供。更确切地说：

- 整体的数据流分析算法，以及从栈中弹出适当数量的值并把它们压回栈中的任务，都在 `Analyzer` 和 `Frame` 类中一次性实现。
- 组合值以及计算值集合并集的任务，由用户定义的 `Interpreter` 和 `Value` 抽象类的子类完成。框架提供了几个预定义的子类，将在后续几节中介绍。

尽管该框架的主要目标是执行数据流分析，`Analyzer` 类也可以构造被分析方法的控制流图。这可以通过覆写该类中默认什么都不做的 `newControlFlowEdge` 和 `newControlFlowExceptionEdge` 方法来完成。其结果可用于进行控制流分析。

> 笔记：
>
> 先看 8.2 的分工：固定部分交给 `Analyzer` 和 `Frame`，它们负责遍历控制流、维护局部变量和操作数栈；可变部分交给 `Interpreter` 和 `Value`，它们负责回答“这条指令产生什么值”“两条路径的值怎么合并”。
>
> 由此得出一个框架图：同一个 `Analyzer` 可以配不同的 `Interpreter`。配 `BasicInterpreter` 能找不可达代码，配 `BasicVerifier` 能查基本类型错误，配自定义解释器就能做更具体的分析。控制流分析则从 `newControlFlowEdge` 这两个回调切入。

### 8.2.1 基本数据流分析

`BasicInterpreter` 类是 `Interpreter` 抽象类的预定义子类之一。它使用 `BasicValue` 类中定义的七个值集合来模拟字节码指令的效果：

- `UNINITIALIZED_VALUE` 表示「所有可能的值」。
- `INT_VALUE` 表示「所有 `int`、`short`、`byte`、`boolean` 或 `char` 值」。
- `FLOAT_VALUE` 表示「所有 `float` 值」。
- `LONG_VALUE` 表示「所有 `long` 值」。
- `DOUBLE_VALUE` 表示「所有 `double` 值」。
- `REFERENCE_VALUE` 表示「所有对象和数组值」。
- `RETURNADDRESS_VALUE` 用于子例程（参见附录 A.2）。

这个解释器本身不太有用（方法帧已经提供了这样的信息，而且更详细——参见 3.1.5 节），但它可以作为「空」的 `Interpreter` 实现来构造 `Analyzer`。这样的分析器随后可以用来检测方法中的不可达代码。实际上，即使在跳转指令中同时沿两个分支走，也不可能到达那些无法从第一条指令到达的代码。其结果是：在一次分析之后，无论 `Interpreter` 的实现是什么，对于无法到达的指令，所计算出的帧——由 `Analyzer.getFrames` 方法返回——都是 `null`。利用这一性质可以很容易地实现一个 `RemoveDeadCodeAdapter` 类（还有更高效的做法，但需要写更多代码）：

```java
public class RemoveDeadCodeAdapter extends MethodVisitor {
    String owner;
    MethodVisitor next;
    public RemoveDeadCodeAdapter(String owner, int access, String name,
            String desc, MethodVisitor mv) {
        super(ASM4, new MethodNode(access, name, desc, null, null));
        this.owner = owner;
        next = mv;
    }
    @Override public void visitEnd() {
        MethodNode mn = (MethodNode) mv;
        Analyzer<BasicValue> a =
                new Analyzer<BasicValue>(new BasicInterpreter());
        try {
            a.analyze(owner, mn);
            Frame<BasicValue>[] frames = a.getFrames();
            AbstractInsnNode[] insns = mn.instructions.toArray();
            for (int i = 0; i < frames.length; ++i) {
                if (frames[i] == null && !(insns[i] instanceof LabelNode)) {
                    mn.instructions.remove(insns[i]);
                }
            }
        } catch (AnalyzerException ignored) {
        }
        mn.accept(next);
    }
}
```

当与 7.1.5 节中的 `OptimizeJumpAdapter` 结合使用时，由跳转优化器引入的死代码就会被删除。例如，在 `checkAndSetF` 方法上使用这条适配器链会得到：

```text
// 经过 OptimizeJump 之后         // 经过 RemoveDeadCode 之后
  ILOAD 1                         ILOAD 1
  IFLT label                      IFLT label
  ALOAD 0                         ALOAD 0
  ILOAD 1                         ILOAD 1
  PUTFIELD ...                    PUTFIELD ...
  RETURN                          RETURN
label:                            label:
F_SAME                            F_SAME
  NEW ...                         NEW ...
  DUP                             DUP
  INVOKESPECIAL ...               INVOKESPECIAL ...
  ATHROW                          ATHROW
end:                              end:
F_SAME
  RETURN
```

注意，死标签不会被删除。这是有意为之的：它确实不会改变生成的代码，却可以避免删除一个虽然不可达、但可能被 `LocalVariableNode` 之类引用的标签。

> 笔记：
>
> 先看 `BasicInterpreter` 本身：它几乎不做事，它的价值是给 `Analyzer` 当「空」的解释器，用来验证一个贯穿全章的结论：**分析后帧为 null 的指令，就是不可达指令**。想一想就明白——分析器连跳转的两个分支都会模拟，凡能到达的路径都走过了，走不到的就是永远执行不到的。
>
> `RemoveDeadCodeAdapter` 就在 `visitEnd` 里利用这一点：把攒下来的 `MethodNode` 交给 `Analyzer`，遍历 `getFrames()`，把帧为 null 且不是标签的指令删掉。**标签要特意保留**——死标签虽然不改变代码，却可能被 `LocalVariableNode` 之类引用。删完再把改好的方法原样转发给链上的下一个访问器，让变换继续接力。
>
> 另外留意那句 `catch (AnalyzerException ignored) {}`：死代码删除属于「可选优化」，分析失败就悄悄跳过、不干扰主流程，这是这类适配器的常见策略。
>
> - [RemoveDeadCode完整代码](#RemoveDeadCode完整代码)

### 8.2.2 基本数据流校验器

`BasicVerifier` 类扩展了 `BasicInterpreter` 类。它使用同样的七个集合，但与 `BasicInterpreter` 不同，它会检查指令是否被正确使用。例如，它会检查 `IADD` 指令的操作数是否为 `INTEGER_VALUE` 值（而 `BasicInterpreter` 只是返回结果，即 `INTEGER_VALUE`）。这个类可以在开发类生成器或适配器期间用于调试目的，如 3.3 节所述。例如，这个类可以检测出 `ISTORE 1 ALOAD 1` 序列是无效的。它可以像下面这样被纳入一个工具方法适配器中（在实践中，使用 `CheckMethodAdapter` 类更简单，它可以被配置为使用 `BasicVerifier`）：

```java
public class BasicVerifierAdapter extends MethodVisitor {
    String owner;
    MethodVisitor next;
    public BasicVerifierAdapter(String owner, int access, String name,
            String desc, MethodVisitor mv) {
        super(ASM4, new MethodNode(access, name, desc, null, null));
        this.owner = owner;
        next = mv;
    }
    @Override public void visitEnd() {
        MethodNode mn = (MethodNode) mv;
        Analyzer<BasicValue> a =
                new Analyzer<BasicValue(new BasicVerifier());
        try {
            a.analyze(owner, mn);
        } catch (AnalyzerException e) {
            throw new RuntimeException(e.getMessage());
        }
        mn.accept(next);
    }
}
```

> 笔记：
>
> 先看 `BasicVerifier` 和 `BasicInterpreter` 的关系：二者用同一套七个值，区别只在**态度**：`BasicInterpreter` 对操作数类型不闻不问、模拟完就返回结果；`BasicVerifier` 则边模拟边检查——`IADD` 的操作数必须是 int、`ALOAD` 的局部变量必须是引用，不对就抛 `AnalyzerException`。
>
> 它的典型用途是开发期调试：在类生成器或适配器初期把它挂进变换链，非法字节码当场败露，而不是等 JVM 的类验证器在更晚、更难定位的时候才报错。原文的 `BasicVerifierAdapter` 把验证失败重新包装成 `RuntimeException` 抛出，让调用方无法忽视。日常实践更省事的是直接用 `CheckMethodAdapter`（asm-util 包），它内部就能配置成使用 `BasicVerifier`。
>
> - [BasicVerifier完整代码](#BasicVerifier完整代码)

### 8.2.3 简单数据流校验器

`SimpleVerifier` 类扩展了 `BasicVerifier` 类。它使用更多的集合来模拟字节码指令的执行：实际上，每个类都由它自己的集合表示，该集合代表这个类的所有可能对象。因此它可以检测出更多错误，例如在可能值为「所有 `Thread` 类型的对象」的对象上调用 `String` 类中定义的方法。这个类使用 Java 反射 API 来执行与类层次结构相关的校验和计算。因此它会把方法所引用的类加载到 JVM 中。这一默认行为可以通过覆写这个类的 `protected` 方法来改变。

与 `BasicVerifier` 一样，这个类可以在开发类生成器或适配器期间使用，以便更容易地发现缺陷。但它也可以用于其他目的。一个例子是删除方法中不必要的强制类型转换的变换：如果这个分析器发现某条 `CHECKCAST` 指令的操作数是「所有 `from` 类型的对象」这一值集合，并且 `to` 是 `from` 的超类，那么这条 `CHECKCAST` 指令就是不必要的，可以被删除。该变换的实现如下：

```java
public class RemoveUnusedCastTransformer extends MethodTransformer {
    String owner;
    public RemoveUnusedCastTransformer(String owner,
            MethodTransformer mt) {
        super(mt);
        this.owner = owner;
    }
    @Override public MethodNode transform(MethodNode mn) {
        Analyzer<BasicValue> a =
                new Analyzer<BasicValue>(new SimpleVerifier());
        try {
            a.analyze(owner, mn);
            Frame<BasicValue>[] frames = a.getFrames();
            AbstractInsnNode[] insns = mn.instructions.toArray();
            for (int i = 0; i < insns.length; ++i) {
                AbstractInsnNode insn = insns[i];
                if (insn.getOpcode() == CHECKCAST) {
                    Frame f = frames[i];
                    if (f != null && f.getStackSize() > 0) {
                        Object operand = f.getStack(f.getStackSize() - 1);
                        Class<?> to = getClass(((TypeInsnNode) insn).desc);
                        Class<?> from = getClass(((BasicValue) operand).getType());
                        if (to.isAssignableFrom(from)) {
                            mn.instructions.remove(insn);
                        }
                    }
                }
            }
        } catch (AnalyzerException ignored) {
        }
        return mt == null ? mn : mt.transform(mn);
    }
    private static Class<?> getClass(String desc) {
        try {
            return Class.forName(desc.replace('/', '.'));
        } catch (ClassNotFoundException e) {
            throw new RuntimeException(e.toString());
        }
    }
    private static Class<?> getClass(Type t) {
        if (t.getSort() == Type.OBJECT) {
            return getClass(t.getInternalName());
        }
        return getClass(t.getDescriptor());
    }
}
```

然而，对于 Java 6 的类（或者使用 `COMPUTE_FRAMES` 升级到 Java 6 的类），用 Core API 借助 `AnalyzerAdapter` 来做同样的事情会更简单，效率也高得多：

```java
public class RemoveUnusedCastAdapter extends MethodVisitor {
    public AnalyzerAdapter aa;
    public RemoveUnusedCastAdapter(MethodVisitor mv) {
        super(ASM4, mv);
    }
    @Override public void visitTypeInsn(int opcode, String desc) {
        if (opcode == CHECKCAST) {
            Class<?> to = getClass(desc);
            if (aa.stack != null && aa.stack.size() > 0) {
                Object operand = aa.stack.get(aa.stack.size() - 1);
                if (operand instanceof String) {
                    Class<?> from = getClass((String) operand);
                    if (to.isAssignableFrom(from)) {
                        return;
                    }
                }
            }
        }
        mv.visitTypeInsn(opcode, desc);
    }
    private static Class getClass(String desc) {
        try {
            return Class.forName(desc.replace('/', '.'));
        } catch (ClassNotFoundException e) {
            throw new RuntimeException(e.toString());
        }
    }
}
```

> 笔记：
>
> 先看 `SimpleVerifier` 比 `BasicVerifier` 多出的信息量：它不再只把所有引用都看成同一个集合，而是把每个类看成自己的集合。因此它能发现“在 `Thread` 对象上调用 `String` 方法”这类更具体的错误。
>
> 再看两个删除强转的实现：Tree API 版本先分析完整方法，再在 `CHECKCAST` 处读取对应帧；Core API 版本借助 `AnalyzerAdapter` 在访问指令时直接看当前栈。由此得出原文的取舍：Java 6 类或使用 `COMPUTE_FRAMES` 时，用 Core API 的 `AnalyzerAdapter` 更简单也更快。

### 8.2.4 用户自定义数据流分析

假设我们想要检测对可能为 null 的对象进行的字段访问和方法调用，例如下面这段源代码片段（其中第一行会让某些编译器无法检测出这个 bug，否则它会被检测为「o may not have been initialized」错误）：

```java
Object o = null;
while (...) {
    o = ...;
}
o.m(...); // 可能抛出 NullPointerException！
```

那么我们就需要一种数据流分析，它能够告诉我们：在与最后一行相对应的 `INVOKEVIRTUAL` 指令处，与 `o` 相对应的栈底值可能为 null。为此，我们需要为引用值区分三个集合：包含 null 值的 `NULL` 集合、包含所有非 null 引用值的 `NONNULL` 集合，以及包含所有引用值的 `MAYBENULL` 集合。接下来我们只需考虑：`ACONST_NULL` 把 `NULL` 集合压入操作数栈，而其他所有把引用值压入栈的指令压入的都是 `NONNULL` 集合（换句话说，我们认为任何字段访问或方法调用的结果都不是 null——如果不对程序中所有类做全局分析，我们无法做得更好）。`MAYBENULL` 集合用于表示 `NULL` 集合与 `NONNULL` 集合的并集。

上述规则必须在一个自定义的 `Interpreter` 子类中实现。我们当然可以从头实现它，但也可以（而且容易得多）通过扩展 `BasicInterpreter` 类来实现。事实上，如果我们认为 `BasicValue.REFERENCE_VALUE` 对应于 `NONNULL` 集合，那么我们只需覆写模拟 `ACONST_NULL` 执行的方法，使其返回 `NULL`，以及计算集合并集的方法：

```java
class IsNullInterpreter extends BasicInterpreter {
    public final static BasicValue NULL = new BasicValue(null);
    public final static BasicValue MAYBENULL = new BasicValue(null);
    public IsNullInterpreter() {
        super(ASM4);
    }
    @Override public BasicValue newOperation(AbstractInsnNode insn) {
        if (insn.getOpcode() == ACONST_NULL) {
            return NULL;
        }
        return super.newOperation(insn);
    }
    @Override public BasicValue merge(BasicValue v, BasicValue w) {
        if (isRef(v) && isRef(w) && v != w) {
            return MAYBENULL;
        }
        return super.merge(v, w);
    }
    private boolean isRef(Value v) {
        return v == REFERENCE_VALUE || v == NULL || v == MAYBENULL;
    }
}
```

有了它，使用这个 `IsNullInterpreter` 来检测可能导致潜在空指针异常的指令就很容易了：

```java
public class NullDereferenceAnalyzer {
    public List<AbstractInsnNode> findNullDereferences(String owner,
        MethodNode mn) throws AnalyzerException {
        List<AbstractInsnNode> result = new ArrayList<AbstractInsnNode>();
        Analyzer<BasicValue> a =
            new Analyzer<BasicValue>(new IsNullInterpreter());
        a.analyze(owner, mn);
        Frame<BasicValue>[] frames = a.getFrames();
        AbstractInsnNode[] insns = mn.instructions.toArray();
        for (int i = 0; i < insns.length; ++i) {
            AbstractInsnNode insn = insns[i];
            if (frames[i] != null) {
                Value v = getTarget(insn, frames[i]);
                if (v == NULL || v == MAYBENULL) {
                    result.add(insn);
                }
            }
        }
        return result;
    }
    private static BasicValue getTarget(AbstractInsnNode insn,
        Frame<BasicValue> f) {
        switch (insn.getOpcode()) {
        case GETFIELD:
        case ARRAYLENGTH:
        case MONITORENTER:
        case MONITOREXIT:
            return getStackValue(f, 0);
        case PUTFIELD:
            return getStackValue(f, 1);
        case INVOKEVIRTUAL:
        case INVOKESPECIAL:
        case INVOKEINTERFACE:
            String desc = ((MethodInsnNode) insn).desc;
            return getStackValue(f, Type.getArgumentTypes(desc).length);
        }
        return null;
    }
    private static BasicValue getStackValue(Frame<BasicValue> f,
        int index) {
        int top = f.getStackSize() - 1;
        return index <= top ? f.getStack(top - index) : null;
    }
}
```

`findNullDereferences` 方法用 `IsNullInterpreter` 分析给定的方法节点。然后它对每条指令进行测试，检查其引用操作数（如果有的话）的可能取值集合是 `NULL` 集合还是 `NONNULL` 集合。如果是这种情况，那么该指令可能导致空指针异常，因此它被加入由该方法返回的这类指令的列表中。

`getTarget` 方法返回帧 `f` 中与 `insn` 的对象操作数相对应的 `Value`，如果 `insn` 没有对象操作数则返回 null。它的主要作用是计算该值相对于操作数栈顶的偏移量，而这取决于指令的类型。

> 笔记：
>
> 先看 8.2.4，它是「用户自定义 `Interpreter`」的样板，拆开看只需覆写两处。**`newOperation`**：把 `ACONST_NULL` 的结果压成 `NULL` 集合，其余照旧；**`merge`**：两条分支汇合时，两边都是引用值且不是同一个集合，就并成 `MAYBENULL`。三值集合 { NULL, NONNULL, MAYBENULL } 正是「值集合可以做得比七值更精细」的示范。
>
> 再记住一个分工：**`Analyzer` 管「怎么走、走到哪」（帧的推进与汇合），`Interpreter` 管「值是什么」（每条指令的结果与并集）**。`NullDereferenceAnalyzer` 只是分析完成后逐条指令比对对象操作数，属于锦上添花。
>
> 版本差异值得留意：ASM 4 里所有对象值都是 `REFERENCE_VALUE` 单例，判断引用很容易；ASM 9 的 `BasicInterpreter` 会给每个对象类型都造带具体类型的 `BasicValue`，判断引用需要连类型一起看——同步到新版本时这是最容易踩的坑。
>
> - [IsNullInterpreter完整代码](#IsNullInterpreter完整代码)

### 8.2.5 控制流分析

控制流分析可以有许多应用。一个简单的例子是计算方法的圈复杂度。该度量的定义是：控制流图中边的数目减去节点的数目，再加二。例如，`checkAndSetF` 方法的控制流图见 8.1.2 节，其圈复杂度为 11 − 12 + 2 = 1。这个度量可以很好地反映一个方法的「复杂度」（该数值与方法中 bug 的平均数量之间存在相关性）。它还给出了「正确地」测试一个方法所必需的推荐测试用例数量。

计算该度量的算法可以用 ASM 的分析框架来实现（也存在更高效的做法，只基于 Core API，但它们需要编写更多代码）。第一步是构造控制流图。正如本章开头所说，这可以通过覆写 `Analyzer` 类的 `newControlFlowEdge` 方法来完成。该类用 `Frame` 对象来表示节点。如果我们想把图存储在这些对象中，就需要扩展 `Frame` 类：

```java
class Node<V extends Value> extends Frame<V> {
    Set< Node<V> > successors = new HashSet< Node<V> >();
    public Node(int nLocals, int nStack) {
        super(nLocals, nStack);
    }
    public Node(Frame<? extends V> src) {
        super(src);
    }
}
```

然后我们可以提供一个构造我们的控制流图的 `Analyzer` 子类，并用它的结果来计算边的数目、节点的数目，最后得到圈复杂度：

```java
public class CyclomaticComplexity {
    public int getCyclomaticComplexity(String owner, MethodNode mn)
         throws AnalyzerException {
        Analyzer<BasicValue> a =
            new Analyzer<BasicValue>(new BasicInterpreter()) {
            protected Frame<BasicValue> newFrame(int nLocals, int nStack) {
                return new Node<BasicValue>(nLocals, nStack);
            }
            protected Frame<BasicValue> newFrame(
                Frame<? extends BasicValue> src) {
                return new Node<BasicValue>(src);
            }
            protected void newControlFlowEdge(int src, int dst) {
                Node<BasicValue> s = (Node<BasicValue>) getFrames()[src];
                s.successors.add((Node<BasicValue>) getFrames()[dst]);
            }
        };
        a.analyze(owner, mn);
        Frame<BasicValue>[] frames = a.getFrames();
        int edges = 0;
        int nodes = 0;
        for (int i = 0; i < frames.length; ++i) {
            if (frames[i] != null) {
                edges += ((Node<BasicValue>) frames[i]).successors.size();
                nodes += 1;
            }
        }
        return edges - nodes + 2;
    }
}
```

> 笔记：
>
> 先看 8.2.5 要算的量：圈复杂度衡量方法的「分支密度」：**边数 − 点数 + 2**。线性方法为 1，每多一个 if/for/switch 判定点就 +1，所以它也约等于「覆盖所有独立路径所需的最少测试用例数」——原文说它能反映复杂度、并给出「推荐的测试用例数量」，出处就在这里。
>
> `Node` 类不过是给 `Frame` 挂了一个后继集合，真正的功夫在覆写 `Analyzer`：**`newFrame` 保证每个节点都是 `Node`，`newControlFlowEdge` 在建图的同时登记边**，分析结束数边、数点、套公式。两个细节值得留意：`newControlFlowEdge` 在目标帧**已经建立**之后才回调，回调里取两端帧都是安全的；`src == dst` 的自环边要过滤（示例代码里特意做了这道判断），否则会把「绕回自身」的边也数进去。
>
> - [CyclomaticComplexity完整代码](#CyclomaticComplexity完整代码)

## 附加

### RemoveDeadCode完整代码

```java
import org.objectweb.asm.MethodVisitor;
import org.objectweb.asm.Opcodes;
import org.objectweb.asm.tree.AbstractInsnNode;
import org.objectweb.asm.tree.FieldInsnNode;
import org.objectweb.asm.tree.FrameNode;
import org.objectweb.asm.tree.InsnNode;
import org.objectweb.asm.tree.JumpInsnNode;
import org.objectweb.asm.tree.LabelNode;
import org.objectweb.asm.tree.LineNumberNode;
import org.objectweb.asm.tree.MethodInsnNode;
import org.objectweb.asm.tree.MethodNode;
import org.objectweb.asm.tree.TypeInsnNode;
import org.objectweb.asm.tree.VarInsnNode;
import org.objectweb.asm.tree.analysis.Analyzer;
import org.objectweb.asm.tree.analysis.AnalyzerException;
import org.objectweb.asm.tree.analysis.BasicInterpreter;
import org.objectweb.asm.tree.analysis.BasicValue;
import org.objectweb.asm.tree.analysis.Frame;
import org.objectweb.asm.util.Printer;

/**
 * 8.2.1 完整可运行示例：RemoveDeadCodeAdapter（删除死代码）。
 *
 * 原理：用 Analyzer&lt;BasicValue&gt; 前向分析整个方法，
 * 分析结束后 getFrames() 中为 null 的帧对应的指令就是不可达指令（死代码），
 * 删除它们（标签除外，标签可能被 LocalVariableNode 等引用，删掉会出问题）。
 */
public class RemoveDeadCodeDemo {

    /** 原文 8.2.1 的 RemoveDeadCodeAdapter，改动：ASM4 常量替换为 Opcodes.ASM9 */
    public static class RemoveDeadCodeAdapter extends MethodVisitor {
        String owner;
        MethodVisitor next;

        public RemoveDeadCodeAdapter(String owner, int access, String name,
                                     String desc, MethodVisitor mv) {
            super(Opcodes.ASM9, new MethodNode(access, name, desc, null, null));
            this.owner = owner;
            next = mv;
        }

        @Override
        public void visitEnd() {
            MethodNode mn = (MethodNode) mv;
            Analyzer<BasicValue> a = new Analyzer<>(new BasicInterpreter());
            try {
                a.analyze(owner, mn);
                Frame<BasicValue>[] frames = a.getFrames();
                AbstractInsnNode[] insns = mn.instructions.toArray();
                for (int i = 0; i < frames.length; ++i) {
                    // 帧为 null = 不可达指令；标签要保留（可能被 LocalVariableNode 引用）
                    if (frames[i] == null && !(insns[i] instanceof LabelNode)) {
                        mn.instructions.remove(insns[i]);
                    }
                }
            } catch (AnalyzerException ignored) {
            }
            mn.accept(next);
        }
    }

    /**
     * 手工构造一个带死代码的方法，形状与原文 8.1.2/8.2.1 的 checkAndSetF 类似：
     * 正常分支（this.f = f; return）与异常分支（throw）之外，
     * 末尾还留了一条永远执行不到的 RETURN —— 它就是待删除的死代码。
     */
    public static MethodNode buildMethodWithDeadCode() {
        MethodNode mn = new MethodNode(Opcodes.ASM9, Opcodes.ACC_PUBLIC,
                "checkAndSetF", "(I)V", null, null);
        LabelNode label = new LabelNode(); // 异常分支入口（f < 0 时跳到这里）
        LabelNode end = new LabelNode();   // 死代码标签（保留）
        mn.instructions.add(new VarInsnNode(Opcodes.ILOAD, 1));      // 参数 f
        mn.instructions.add(new JumpInsnNode(Opcodes.IFLT, label));  // f < 0 → label
        mn.instructions.add(new VarInsnNode(Opcodes.ALOAD, 0));      // this
        mn.instructions.add(new VarInsnNode(Opcodes.ILOAD, 1));
        mn.instructions.add(new FieldInsnNode(Opcodes.PUTFIELD, "pkg/Bean", "f", "I"));
        mn.instructions.add(new InsnNode(Opcodes.RETURN));
        mn.instructions.add(label);
        mn.instructions.add(new TypeInsnNode(Opcodes.NEW, "java/lang/IllegalStateException"));
        mn.instructions.add(new InsnNode(Opcodes.DUP));
        mn.instructions.add(new MethodInsnNode(Opcodes.INVOKESPECIAL,
                "java/lang/IllegalStateException", "<init>", "()V", false));
        mn.instructions.add(new InsnNode(Opcodes.ATHROW));
        mn.instructions.add(end);
        mn.instructions.add(new InsnNode(Opcodes.RETURN)); // ← 死代码：ATHROW 之后无人能到达
        mn.visitMaxs(2, 2);
        return mn;
    }

    /** 打印方法的指令清单（标签与帧单独标注，其余显示助记符） */
    public static void printInstructions(String title, MethodNode mn) {
        System.out.println("---- " + title + " ----");
        AbstractInsnNode[] insns = mn.instructions.toArray();
        for (AbstractInsnNode insn : insns) {
            if (insn instanceof LabelNode) {
                System.out.println("标签（保留）");
            } else if (insn instanceof LineNumberNode) {
                // 跳过行号节点
            } else if (insn instanceof FrameNode) {
                System.out.println("帧");
            } else {
                System.out.println("  " + Printer.OPCODES[insn.getOpcode()]);
            }
        }
        System.out.println();
    }

    public static void main(String[] args) {
        // 1. 变换前：方法里含有一条不可达的 RETURN
        MethodNode before = buildMethodWithDeadCode();
        printInstructions("变换前（含死代码）", before);

        // 2. 把 before 重放进 RemoveDeadCodeAdapter，清理结果写到 out
        MethodNode out = new MethodNode(Opcodes.ASM9);
        String owner = "pkg/Bean";
        RemoveDeadCodeAdapter adapter = new RemoveDeadCodeAdapter(owner,
                Opcodes.ACC_PUBLIC, "checkAndSetF", "(I)V", out);
        before.accept(adapter); // 重放方法事件，visitEnd 中触发分析并删除死代码

        printInstructions("变换后（死代码已删除）", out);

        int beforeCount = before.instructions.size();
        int afterCount = out.instructions.size();
        System.out.println("指令总数：" + beforeCount + " → " + afterCount
                + "（删除了 " + (beforeCount - afterCount) + " 条不可达指令）");
        System.out.println("最末尾的 RETURN 是不可达代码，已被删除；标签 end 被保留。");
    }
}
```

```
---- 变换前（含死代码） ----
  ILOAD
  IFLT
  ALOAD
  ILOAD
  PUTFIELD
  RETURN
标签（保留）
  NEW
  DUP
  INVOKESPECIAL
  ATHROW
标签（保留）
  RETURN

---- 变换后（死代码已删除） ----
  ILOAD
  IFLT
  ALOAD
  ILOAD
  PUTFIELD
  RETURN
标签（保留）
  NEW
  DUP
  INVOKESPECIAL
  ATHROW
标签（保留）

指令总数：13 → 12（删除了 1 条不可达指令）
最末尾的 RETURN 是不可达代码，已被删除；标签 end 被保留。
```


### BasicVerifier完整代码

```java
import org.objectweb.asm.ClassReader;
import org.objectweb.asm.MethodVisitor;
import org.objectweb.asm.Opcodes;
import org.objectweb.asm.tree.ClassNode;
import org.objectweb.asm.tree.InsnNode;
import org.objectweb.asm.tree.MethodNode;
import org.objectweb.asm.tree.analysis.Analyzer;
import org.objectweb.asm.tree.analysis.AnalyzerException;
import org.objectweb.asm.tree.analysis.BasicValue;
import org.objectweb.asm.tree.analysis.BasicVerifier;
import org.objectweb.asm.util.CheckMethodAdapter;

/**
 * 8.2.2 完整可运行示例：BasicVerifierAdapter（基本数据流校验器）。
 *
 * BasicVerifier 与 BasicInterpreter 用同一套七个值集合，
 * 但它在模拟指令时会顺手检查操作数的类型是否合法，
 * 例如 IADD 的两个操作数必须是 int、ALOAD 加载的局部变量必须是引用。
 * 检查不通过会抛 AnalyzerException，可用来在开发生成器/适配器期间抓 bug。
 */
public class BasicVerifierDemo {

    /** 被测类：javac 生成的、完全合法的方法 */
    public static class Sample {
        static int foo(int x) {
            if (x > 0) {
                return x;
            }
            return -x;
        }
    }

    /** 原文 8.2.2 的 BasicVerifierAdapter，改动：ASM4 常量替换为 Opcodes.ASM9 */
    public static class BasicVerifierAdapter extends MethodVisitor {
        String owner;
        MethodVisitor next;

        public BasicVerifierAdapter(String owner, int access, String name,
                                    String desc, MethodVisitor mv) {
            super(Opcodes.ASM9, new MethodNode(access, name, desc, null, null));
            this.owner = owner;
            next = mv;
        }

        @Override
        public void visitEnd() {
            MethodNode mn = (MethodNode) mv;
            Analyzer<BasicValue> a = new Analyzer<>(new BasicVerifier());
            try {
                a.analyze(owner, mn);
            } catch (AnalyzerException e) {
                throw new RuntimeException(e.getMessage());
            }
            mn.accept(next);
        }
    }

    /**
     * 手工构造一个非法方法：(II)I 表示两个 int 参数。
     * 序列 ISTORE 1（把 int 存进局部变量 1）之后马上 ALOAD 1
     * （把同一个局部变量当对象加载）—— 这就是原文说的非法序列。
     */
    public static MethodNode buildInvalidMethod() {
        MethodNode mn = new MethodNode(Opcodes.ASM9,
                Opcodes.ACC_PUBLIC + Opcodes.ACC_STATIC, "bad", "(II)I", null, null);
        mn.instructions.add(new org.objectweb.asm.tree.VarInsnNode(Opcodes.ILOAD, 1));  // 加载参数 b（int）
        mn.instructions.add(new org.objectweb.asm.tree.VarInsnNode(Opcodes.ISTORE, 1)); // 存回 b（仍是 int）
        mn.instructions.add(new org.objectweb.asm.tree.VarInsnNode(Opcodes.ALOAD, 1));  // ❌ 把 int 当引用加载
        mn.instructions.add(new InsnNode(Opcodes.IRETURN));
        mn.visitMaxs(1, 2);
        return mn;
    }

    /** 用 BasicVerifierAdapter 验证某个方法，返回校验结果字符串 */
    static String verify(String owner, MethodNode mn) {
        BasicVerifierAdapter adapter = new BasicVerifierAdapter(owner,
                mn.access, mn.name, mn.desc, new MethodNode(Opcodes.ASM9));
        try {
            mn.accept(adapter);
            return "验证通过：方法字节码合法";
        } catch (RuntimeException e) {
            return "验证失败：" + e.getMessage();
        }
    }

    public static void main(String[] args) throws Exception {
        // 1. 从磁盘读入真实类，取出 foo 方法验证（合法字节码应通过）
        ClassReader cr = new ClassReader("BasicVerifierDemo$Sample");
        ClassNode cn = new ClassNode();
        cr.accept(cn, 0);
        MethodNode foo = null;
        for (MethodNode m : cn.methods) {
            if (m.name.equals("foo")) {
                foo = m;
            }
        }
        System.out.println("待验证方法：" + cn.name + "." + foo.name + foo.desc);
        System.out.println("  " + verify(cn.name, foo));

        // 2. 验证手工构造的非法方法（ISTORE 1; ALOAD 1）
        MethodNode bad = buildInvalidMethod();
        System.out.println("待验证方法：" + cn.name + ".bad" + bad.desc);
        System.out.println("  " + verify(cn.name, bad));
        System.out.println("说明：BasicVerifier 抓住了 `ISTORE 1; ALOAD 1` 这种把 int 当引用使用的非法序列。");

        // 3. 顺带演示原文提到的 alternatives：CheckMethodAdapter 也可以配 BasicVerifier
        System.out.println();
        System.out.println("补充：实践中更省事的是用 CheckMethodAdapter（asm-util 包），"
                + "它内部就支持 BasicVerifier 这类检查。用法与上面类似：");
        MethodNode checkTarget = new MethodNode(Opcodes.ASM9);
        new CheckMethodAdapter(checkTarget);
        System.out.println("  CheckMethodAdapter 构造成功（本次不做完整校验演示）。");
    }
}
```

```
待验证方法：BasicVerifierDemo$Sample.foo(I)I
  验证通过：方法字节码合法
待验证方法：BasicVerifierDemo$Sample.bad(II)I
  验证失败：Error at instruction 2: Expected an object reference, but found I
说明：BasicVerifier 抓住了 `ISTORE 1; ALOAD 1` 这种把 int 当引用使用的非法序列。

补充：实践中更省事的是用 CheckMethodAdapter（asm-util 包），它内部就支持 BasicVerifier 这类检查。用法与上面类似：
  CheckMethodAdapter 构造成功（本次不做完整校验演示）。
```


### IsNullInterpreter完整代码

```java
import org.objectweb.asm.ClassReader;
import org.objectweb.asm.Opcodes;
import org.objectweb.asm.Type;
import org.objectweb.asm.tree.AbstractInsnNode;
import org.objectweb.asm.tree.ClassNode;
import org.objectweb.asm.tree.MethodInsnNode;
import org.objectweb.asm.tree.MethodNode;
import org.objectweb.asm.tree.analysis.Analyzer;
import org.objectweb.asm.tree.analysis.AnalyzerException;
import org.objectweb.asm.tree.analysis.BasicInterpreter;
import org.objectweb.asm.tree.analysis.BasicValue;
import org.objectweb.asm.tree.analysis.Frame;
import org.objectweb.asm.tree.analysis.Value;
import org.objectweb.asm.util.Printer;

import java.util.ArrayList;
import java.util.List;

/**
 * 8.2.4 完整可运行示例：用户自定义数据流分析 —— 检测潜在空指针解引用。
 *
 * 思路：定义引用值的三个集合 NULL（确定为空）、NONNULL、MAYBENULL（前两者并集），
 * 在自定义 Interpreter 中把 ACONST_NULL 的结果记为 NULL，
 * 分支汇合时若两个引用值不同则合并为 MAYBENULL；
 * 分析完成后逐条指令检查其对象操作数是否为 NULL / MAYBENULL。
 */
public class IsNullInterpreterDemo {

    /** 被测类：包含两个有风险的方法和一个安全方法 */
    public static class Sample {
        /** 分支汇合处 o 可能是 null 也可能是对象 → MAYBENULL */
        static int mayNull(boolean flag) {
            Object o = null;
            if (flag) {
                o = "x";
            }
            return o.hashCode(); // 可能 NPE
        }

        /** o 确定是 null → NULL */
        static int isNull() {
            Object o = null;
            return o.hashCode(); // 必然 NPE（静态分析视角）
        }

        /** 安全方法：o 必然是对象，不应被报告 */
        static int safe() {
            Object o = new Object();
            return o.hashCode();
        }
    }

    /**
     * 原文 8.2.4 的 IsNullInterpreter。
     * 适配 ASM 9：旧版 ASM4 里所有对象值都是 BasicValue.REFERENCE_VALUE 单例，
     * 9.x 的 BasicInterpreter 会给每个对象类型都造一个带具体类型的 BasicValue，
     * 所以 isRef 需要把「类型是对象/数组」的值也当作引用（NONNULL）。
     */
    public static class IsNullInterpreter extends BasicInterpreter {
        public static final BasicValue NULL = new BasicValue(null);
        public static final BasicValue MAYBENULL = new BasicValue(null);

        /** ASM 9 中必须显式调用带 api 的构造器（无参构造器不允许被子类使用） */
        public IsNullInterpreter() {
            super(Opcodes.ASM9);
        }

        @Override
        public BasicValue newOperation(AbstractInsnNode insn) throws AnalyzerException {
            if (insn.getOpcode() == Opcodes.ACONST_NULL) {
                return NULL;
            }
            return super.newOperation(insn);
        }

        @Override
        public BasicValue merge(BasicValue v, BasicValue w) {
            if (isRef(v) && isRef(w) && v != w) {
                return MAYBENULL;
            }
            return super.merge(v, w);
        }

        private boolean isRef(Value v) {
            if (v == NULL || v == MAYBENULL || v == BasicValue.REFERENCE_VALUE) {
                return true;
            }
            if (v instanceof BasicValue) {
                Type t = ((BasicValue) v).getType();
                return t != null
                        && (t.getSort() == Type.OBJECT || t.getSort() == Type.ARRAY);
            }
            return false;
        }
    }

    /** 原文 8.2.4 的 NullDereferenceAnalyzer：分析后扫描可能空解引用的指令 */
    public static class NullDereferenceAnalyzer {
        public List<AbstractInsnNode> findNullDereferences(String owner, MethodNode mn)
                throws AnalyzerException {
            List<AbstractInsnNode> result = new ArrayList<>();
            Analyzer<BasicValue> a = new Analyzer<>(new IsNullInterpreter());
            a.analyze(owner, mn);
            Frame<BasicValue>[] frames = a.getFrames();
            AbstractInsnNode[] insns = mn.instructions.toArray();
            for (int i = 0; i < insns.length; ++i) {
                AbstractInsnNode insn = insns[i];
                if (frames[i] != null) {
                    BasicValue v = getTarget(insn, frames[i]);
                    if (v == IsNullInterpreter.NULL
                            || v == IsNullInterpreter.MAYBENULL) {
                        result.add(insn);
                    }
                }
            }
            return result;
        }

        /** 返回帧 f 中与 insn 的对象操作数对应的值，没有则返回 null */
        private static BasicValue getTarget(AbstractInsnNode insn, Frame<BasicValue> f) {
            switch (insn.getOpcode()) {
                case Opcodes.GETFIELD:
                case Opcodes.ARRAYLENGTH:
                case Opcodes.MONITORENTER:
                case Opcodes.MONITOREXIT:
                    return getStackValue(f, 0);
                case Opcodes.PUTFIELD:
                    return getStackValue(f, 1);
                case Opcodes.INVOKEVIRTUAL:
                case Opcodes.INVOKESPECIAL:
                case Opcodes.INVOKEINTERFACE:
                    String desc = ((MethodInsnNode) insn).desc;
                    return getStackValue(f, Type.getArgumentTypes(desc).length);
            }
            return null;
        }

        /** 取距栈顶 index 个位置的栈值（0 = 栈顶） */
        private static BasicValue getStackValue(Frame<BasicValue> f, int index) {
            int top = f.getStackSize() - 1;
            return index <= top ? f.getStack(top - index) : null;
        }
    }

    static String describe(AbstractInsnNode insn) {
        if (insn instanceof MethodInsnNode) {
            MethodInsnNode m = (MethodInsnNode) insn;
            return "INVOKEVIRTUAL " + m.owner + "." + m.name + m.desc;
        }
        return Printer.OPCODES[insn.getOpcode()];
    }

    public static void main(String[] args) throws Exception {
        // 用 ClassReader 读入真实类（当前目录下的 IsNullInterpreterDemo$Sample）
        ClassReader cr = new ClassReader("IsNullInterpreterDemo$Sample");
        ClassNode cn = new ClassNode();
        cr.accept(cn, ClassReader.SKIP_DEBUG); // 跳过行号/调试信息，输出更干净

        NullDereferenceAnalyzer analyzer = new NullDereferenceAnalyzer();
        for (MethodNode mn : cn.methods) {
            if (mn.name.equals("<init>")) {
                continue;
            }
            List<AbstractInsnNode> found = analyzer.findNullDereferences(cn.name, mn);
            System.out.println("方法 " + mn.name + mn.desc + "：");
            if (found.isEmpty()) {
                System.out.println("  (未发现潜在空指针解引用)");
            } else {
                for (AbstractInsnNode insn : found) {
                    System.out.println("  可能空指针解引用！<< " + describe(insn));
                }
            }
        }
        System.out.println();
        System.out.println("预期：mayNull（汇合值为 MAYBENULL）和 isNull（确定 NULL）被报告，");
        System.out.println("safe 不被报告。说明自定义 Interpreter + Analyzer 可以识别跨分支的空值传播。");
    }
}
```

```
方法 mayNull(Z)I：
  可能空指针解引用！<< INVOKEVIRTUAL java/lang/Object.hashCode()I
方法 isNull()I：
  可能空指针解引用！<< INVOKEVIRTUAL java/lang/Object.hashCode()I
方法 safe()I：
  (未发现潜在空指针解引用)

预期：mayNull（汇合值为 MAYBENULL）和 isNull（确定 NULL）被报告，
safe 不被报告。说明自定义 Interpreter + Analyzer 可以识别跨分支的空值传播。
```


### CyclomaticComplexity完整代码

```java
import org.objectweb.asm.ClassReader;
import org.objectweb.asm.Opcodes;
import org.objectweb.asm.tree.AbstractInsnNode;
import org.objectweb.asm.tree.ClassNode;
import org.objectweb.asm.tree.FrameNode;
import org.objectweb.asm.tree.LabelNode;
import org.objectweb.asm.tree.LineNumberNode;
import org.objectweb.asm.tree.MethodNode;
import org.objectweb.asm.tree.analysis.Analyzer;
import org.objectweb.asm.tree.analysis.BasicInterpreter;
import org.objectweb.asm.tree.analysis.BasicValue;
import org.objectweb.asm.tree.analysis.Frame;

import java.util.HashSet;
import java.util.Set;

/**
 * 8.2.5/8.2.6 完整可运行示例：CyclomaticComplexity（环复杂度）。
 *
 * 思路（原文 8.2.6 主循环）：让 Analyzer 在用 newControlFlowEdge
 * 回调建立控制流图的途中，把每条控制流边收集进 incoming 集合；
 * 分析结束后统计可达指令（顶点）与边的数量，再套用
 * 环复杂度 = E - V + 2 计算（E、V 均已折算为“真实指令”，
 * 标签/行号等元节点不参与；方法入口边与返回出口边各算一条）。
 */
public class CyclomaticComplexityDemo {

    private static final String OWNER = "CyclomaticComplexityDemo$Sample";

    /** 被测类：三个复杂度递增的方法 */
    public static class Sample {
        static int trivial(int x) {           // 无分支 → 复杂度 1
            return x + 1;
        }

        static int oneBranch(int x) {         // 一个 if → 复杂度 2
            if (x > 0) {
                return x;
            }
            return 0;
        }

        static int nested(int x, int y) {     // for + if/else + 三目 → 复杂度 4
            int sum = 0;
            for (int i = 0; i < x; i++) {
                if (i % 2 == 0) {
                    sum += i;
                } else {
                    sum -= i;
                }
                sum += (y > 0 ? y : -y);
            }
            return sum;
        }
    }

    /** 原文 8.2.5 的 Node 类：带可达性标记的帧（Frame 的子类，与 asm.tree 的类无关） */
    public static class Node<V extends org.objectweb.asm.tree.analysis.Value> extends Frame<V> {
        public boolean reachable = false;

        public Node(int nLocals, int nStack) {
            super(nLocals, nStack);
        }

        public Node(Frame<? extends V> src) {
            super(src);
        }
    }

    /** 是否为真实指令（LabelNode/LineNumberNode/FrameNode 等元节点除外） */
    static boolean isReal(AbstractInsnNode insn) {
        return !(insn instanceof LabelNode)
                && !(insn instanceof LineNumberNode)
                && !(insn instanceof FrameNode);
    }

    /** 计算一个方法的环复杂度 */
    static int cyclomaticComplexity(MethodNode mn) {
        int n = mn.instructions.size();

        // 下标 → 其后的第一条真实指令下标（若自身是真实指令则等于自身；之后没有则为 -1）
        int[] realOf = new int[n];
        int next = -1;
        AbstractInsnNode[] insns = mn.instructions.toArray();
        for (int i = n - 1; i >= 0; i--) {
            if (isReal(insns[i])) {
                next = i;
            }
            realOf[i] = next;
        }

        // 1) Analyzer 前向分析，同时收集控制流边（原始下标：dst -> src 集合）
        final Set<Integer>[] incoming = new Set[n];
        for (int i = 0; i < n; i++) {
            incoming[i] = new HashSet<>();
        }
        Analyzer<BasicValue> a = new Analyzer<BasicValue>(new BasicInterpreter()) {
            @Override
            protected Frame<BasicValue> newFrame(int nLocals, int nStack) {
                return new Node<>(nLocals, nStack);
            }

            @Override
            protected Frame<BasicValue> newFrame(Frame<? extends BasicValue> src) {
                return new Node<>(src);
            }

            @Override
            protected void newControlFlowEdge(int src, int dst) {
                if (src != dst) {
                    incoming[dst].add(src);
                }
            }
        };
        try {
            a.analyze(OWNER, mn);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }

        // 2) 折算到真实指令上：jump → 标签 的边算作 jump → 标签后的第一条真实指令
        Set<Long> edges = new HashSet<>(); // (src << 32) | dst 去重
        int exits = 0;                     // 返回/抛异常指令数（每条对应一条出口边）
        Frame<BasicValue>[] frames = a.getFrames();
        for (int d = 0; d < n; d++) {
            AbstractInsnNode insn = insns[d];
            if (!isReal(insn)) {
                // 元节点：把它收到的边并入其后第一条真实指令
                int rd = realOf[d];
                if (rd < 0) {
                    continue;
                }
                for (int s : incoming[d]) {
                    addEdge(edges, realOf[s], rd);
                }
            } else if (frames[d] != null) {
                // 真实可达指令：收下每条入边（起点也可能是标签，需折算）
                for (int s : incoming[d]) {
                    addEdge(edges, isReal(insns[s]) ? s : realOf[s], d);
                }
                int opcode = insn.getOpcode();
                if ((opcode >= Opcodes.IRETURN && opcode <= Opcodes.RETURN)
                        || opcode == Opcodes.ATHROW) {
                    exits++;
                }
            }
        }

        // 3) 环复杂度 = (边数 + 入口边 + 出口边) - (顶点数 + 虚拟入口出口) + 2 = E - V + 2
        int vertices = 0;
        for (int i = 0; i < n; i++) {
            if (isReal(insns[i]) && frames[i] != null) {
                vertices++;
            }
        }
        int edgeCount = edges.size() + 1 + exits; // +1 = 方法入口边
        int vertexCount = vertices + 2;           // +2 = 虚拟入口、出口
        return edgeCount - vertexCount + 2;
    }

    private static void addEdge(Set<Long> edges, int src, int dst) {
        if (src >= 0 && dst >= 0 && src != dst) {
            edges.add(((long) src << 32) | dst);
        }
    }

    public static void main(String[] args) throws Exception {
        ClassReader cr = new ClassReader(OWNER);
        ClassNode cn = new ClassNode();
        cr.accept(cn, ClassReader.SKIP_DEBUG);

        String[] srcHint = {
                "return x + 1;",
                "if (x > 0) return x; return 0;",
                "for(...){ if(...) sum+=i; else sum-=i; sum+=(y>0?y:-y); }"
        };
        int idx = 0;
        for (MethodNode mn : cn.methods) {
            if (mn.name.equals("<init>")) {
                continue;
            }
            int cc = cyclomaticComplexity(mn);
            System.out.printf("%-9s 环复杂度 = %d    %s%n", mn.name, cc, srcHint[idx++]);
        }
        System.out.println();
        System.out.println("对照：无分支 = 1；每多一个 if/for/三目等判定点，复杂度 +1。");
    }
}
```

```
trivial   环复杂度 = 1    return x + 1;
oneBranch 环复杂度 = 2    if (x > 0) return x; return 0;
nested    环复杂度 = 4    for(...){ if(...) sum+=i; else sum-=i; sum+=(y>0?y:-y); }

对照：无分支 = 1；每多一个 if/for/三目等判定点，复杂度 +1。
```
